// Générateur d'explication pédagogique en fonction du score de corrélation

export function generateExplanation(correlation: number): string {
    // À compléter plus tard
    return "Corrélation à expliquer ici.";
  }